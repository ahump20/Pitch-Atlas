# Pitch Atlas — Supabase (community / Field Notes).
# Both values are PUBLIC by design: the publishable key is meant to ship in the
# browser bundle. Row-Level Security on the database is the real boundary.
# The app falls back to the live project defaults baked into src/lib/supabase.ts,
# so these are only needed to point a build at a different project.
VITE_SUPABASE_URL=https://cloeoulvrrfcbitrjpso.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_jMuBalGcB-qEBIVAYegthg_0Rr4pUgT
