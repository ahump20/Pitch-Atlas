# dependencies
node_modules
node_modules/.tmp
.pnpm-store/

# build output
dist
dist-ssr
out/
build/
coverage

# vite / cache
.vite

# environment
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editors
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# misc local
*.local

# Cloudflare / tooling
.wrangler/
.dev.vars

# local visual-verification screenshots
artifacts/
.playwright-mcp/
pitch-atlas-*.png
# visual-QA scratch: raw captures, crops, and annotation overlays — local only
playwright-*.png
jimp-crop-*.png
mk-*.html
.qa-shots/

# private reference (copyrighted) + design-handoff leftovers — never commit
Pitching-Grips-101.pdf
Baseball example/
# heavy brand source (AI-generated PNGs); optimized webp lives in public/brand/
Brand Image Assets/
# research & source material: copyrighted PDFs, DRM-locked audiobooks, internal notes — never commit
Pitcher Analysis Assets/
# stray nested repo (unrelated agent-skills clone, carries its own .git) — never commit
agent-skills/
# the iOS app is its own repo (ahump20/Pitch-Atlas-iOS, own .git); a local working
# copy may sit here but must never be committed into the web repo - never commit
Pitch-Atlas-iOS/
# packaged skill/plugin artifacts (built elsewhere, not product source) — never commit
claude-plugin/
# Austin's raw grip source photos — converted to webp for the grip library, not shipped
My Pitch Grips/
pitch-atlas-softball/

# redesign research screenshots (local only)
.redesign-research/
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# agent session + handoff scratch (local only — never commit)
.claude/
.codex/
BRAIN HANDOFF.md
pitch-atlas-founding-conversation.md

# loose scratch captures + design-reference research (local only — never commit)
scratchpad_*.png
# stray BlazeCraft render frames (belong to the BlazeCraft project, not Pitch Atlas)
blazecraft-*.png
*strike zone visualization example*
Sluggers Hit Card Inspiration examples/
